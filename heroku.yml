build:
  docker:
    worker: ./Dockerfile
    beat: ./Dockerfile

setup:
  addons:
    - plan: heroku-redis:hobby-dev
      as: REDIS
  config: 
    REDIS_URL: ${REDIS_URL}
    REDIS_TLS_URL: ${REDIS_TLS_URL}
    MONGODB_URL: ${MONGODB_URL}
